---
format: 5
course: vue
---

# vue

**Front-end with Vue** · `co-2cvbh54f` · 80 h declared · intermediate · 22 lessons · `frontend` · paid

## Reach

In **1 track** — `frontend`(6, choice *Vue*).

**Reached through a choice**, never in sequence — so **no course after that fork may assume it was taken**.

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `javascript` and `git`.

**Leaves ready:** **nothing.** No course requires it.

## Shape

| | |
|---|---|
| declared hours | 80 h |
| lessons | 22 |
| **hours per lesson** | **3.64** |
| section budget | ~171, about 7.8 a lesson |
| exercises | ~800, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **Node and a browser** |
| browser · database | **yes** · no |
| exercises **blocked** | **~550 (70%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~60 — reactivity compared with React's, the composable lifecycle, scoped slots, Nuxt's payload crossing |

## Ageing

**Low to moderate.** The Composition API is settled; lesson 11 keeps the Options API because existing code is full of it, which is the same honesty `angular` shows.

## Flags

**1 ·** **Eighty hours, twenty-two lessons, 7.8 sections each — the middle of the four**, and the only one whose lesson 17 is about a build tool it produced (*"Vite: written by Vue's author, now used by everyone"*), which the other three all use.

**2 ·** **Its last lesson looks at the others.** That is the same shape `react-ts`, `vue`, `angular` and `svelte` all carry, and it is the catalogue answering the choice problem inside the material instead of asking for a mechanism — see the register.

**3 ·** **Lessons 18 to 20 are Nuxt**, and `front-performance` — three positions later, after the fork — teaches Next, Nuxt, SvelteKit and Angular SSR together. **A Vue student meets Nuxt twice**, here in depth and there in comparison, which is the right order and worth writing to rather than trimming.
