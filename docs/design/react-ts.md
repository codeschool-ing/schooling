---
format: 5
course: react-ts
---

# react-ts

**Front-end with React and TypeScript** · `co-nr4b68g3` · 90 h declared · intermediate · 24 lessons · `frontend` · paid

## Reach

In **1 track** — `frontend`(6, choice *React + TypeScript*).

**Reached through a choice**, never in sequence — so **no course after that fork may assume it was taken**.

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `javascript` and `git`.

**Leaves ready:** **nothing.** No course requires it.

## Shape

| | |
|---|---|
| declared hours | 90 h |
| lessons | 24 |
| **hours per lesson** | **3.75** |
| section budget | ~193, about 8.0 a lesson |
| exercises | ~910, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **Node and a browser** |
| browser · database | **yes** · no |
| exercises **blocked** | **~600 (70%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~65 — reconciliation, the hook rules drawn as violations, the client boundary, a re-render cascade |

## Ageing

**Moderate to severe.** Server Components and Actions (lessons 15 and 16) are the newest and least settled things in the batch.

## Flags

**1 ·** **Ninety hours — joint largest in the catalogue** — and the most expensive of the four framework options, tied with `angular`. The fork's options run 70 to 90 hours, a 1.29× spread; **compared with `backend`'s 2.07× that is a fork of genuine equals.**

**2 ·** **Its last lesson looks at the others.** That is the same shape `react-ts`, `vue`, `angular` and `svelte` all carry, and it is the catalogue answering the choice problem inside the material instead of asking for a mechanism — see the register.

**3 ·** **Lesson 1 and lesson 24 are a matched pair** — *"Why React is a library where Angular is a framework"* and *"A look at Angular, Vue and Svelte, from where React stands"*. The course opens and closes by placing itself among the alternatives, which is the strongest version of the pattern in the batch.
