---
format: 5
course: networks-automation
---

# networks-automation

**Network Automation** · `co-7wyz57ha` · 60 h declared · intermediate · 15 lessons · `infra` · paid

## Reach

In **1 track** — `networks-infra`(10).

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `python` **and** `networks-security` — a language and a network to point it at. It is the only course in the category with a language prerequisite.

**Leaves ready:** nothing.

## Shape

| | |
|---|---|
| declared hours | 60 h |
| lessons | 15 |
| **hours per lesson** | **4.00** |
| section budget | ~129, about 8.6 a lesson |
| exercises | ~610, at the catalogue's density |


## Execution

| | |
|---|---|
| runtime | **Python and a network topology, together** |
| browser · database | no · no |
| exercises **blocked** | **0 as written**, and this is the course in `infra` where that phrasing is most strained |
| exercises that would **improve** | **nearly all of it.** The subject is running code against devices |
| diagrams to draw | ~20 — the inventory model, the describe/validate/apply cycle |

## Ageing

**Moderate.** The libraries move faster than the protocols.

## Flags

**1 ·** **The only course in the category that needs two sandboxes at once** — a language runtime and an environment. It is therefore the last one in `infra` that becomes writable, and it should be planned as such rather than discovered.

**2 ·** **Requires `python`, which is a choice option in `devops`.** Here it is a hard prerequisite in `networks-infra`, where `python` does not appear at all — worth checking against the track before this course is written.
