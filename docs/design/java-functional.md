---
format: 5
course: java-functional
---

# java-functional

**Java: Functional and Concurrent** · `co-gjahvk1c` · 70 h declared · intermediate · 26 lessons · `programming` · paid

## Reach

In **1 track** — `backend`(5, choice *Java*).

**Reached through a choice**, never in sequence — so **no course after that fork may assume it was taken**.

**Depends on it:** `java-back`

## Assumes, and leaves ready

**Assumes:** `java` — classes, generics, collections and exceptions.

**Leaves ready:** streams, concurrency and the standard library, for `java-back`.

## Shape

| | |
|---|---|
| declared hours | 70 h |
| lessons | 26 |
| **hours per lesson** | **2.69** |
| section budget | ~150, about 5.8 a lesson |
| exercises | ~700, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **a JDK** |
| browser · database | no · no |
| exercises **blocked** | **~500 (70%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~60 — a stream pipeline, `Optional` chained, the memory model with visibility, executors and pools, virtual threads against platform ones |

## Ageing

**Moderate at one point.** Lesson 17 is virtual threads and lesson 18 structured concurrency — the newest things in Java and still settling, which is exactly why they belong here and will need revisiting.

## Flags

**1 ·** **Its title says functional and half of it is concurrency and I/O.** Lessons 1 to 10 are lambdas, streams and `Optional`; 11 to 26 are threads, files, dates, networking, regex and cryptography. **Third course in this batch that is two courses in one file**, after `api-mobile-automation` and `go-concurrency`, and the same structural note applies.

**2 ·** **Lesson 25 teaches cryptography, and the catalogue has a 50-hour course on it.** `cryptography` is in `security`, `devsecops` and `dba`, and `backend` reaches none of them — so the overview is a Java student's only exposure and is correctly placed. Fourth instance of a compressed overview, and the second that is unambiguously right.

**3 ·** **The middle of the 220-hour Java path**, one dependent, one track, one fork option.
