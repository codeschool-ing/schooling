---
format: 5
course: architecture
---

# architecture

**Systems Architecture** · `co-ax5xzf7h` · 80 h declared · advanced · 20 lessons · `backend` · paid

## Reach

In **1 track** — `backend`(12).

**Depends on it:** `scale`

## Assumes, and leaves ready

**Assumes:** `docker` — containers, because every pattern here is deployed as several of something.

**Leaves ready:** the distributed vocabulary, for `scale`.

## Shape

| | |
|---|---|
| declared hours | 80 h |
| lessons | 20 |
| **hours per lesson** | **4.00** |
| section budget | ~171, about 8.6 a lesson |
| exercises | ~800, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **containers and a message broker** |
| browser · database | no · **yes** |
| exercises **blocked** | **~500 (65%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | **~95** — monolith against microservices, each resilience pattern drawn once, saga compensating, CQRS split, the strangler over time. Among the heaviest in the catalogue |

## Ageing

**Low.** These patterns are twenty years old and the names are stable. Lesson 17 names three search engines.

## Flags

**1 ·** **It overlaps `design-patterns` and `enterprise-software` and no student meets two of them.** CQRS, event sourcing, CAP and the strangler appear in all three — here in `backend`(12), there in `software-architecture`(2 and 11). But `software-architecture` **continues** `backend`, so a student on it has taken *this* course and then meets the other two. **The overlap is real and the ordering makes it deliberate reinforcement** — which only shows with all three sheets side by side.

**2 ·** **Twenty lessons at 8.6 sections each and ~95 diagrams**, the third-heaviest illustration load in the catalogue after `architecture-modeling` and `visualization`. Every resilience pattern is a picture and none of them is optional.

**3 ·** **Position 12 of `backend` with `scale` behind it**, and the last two courses of the track before the portfolio. Whatever the four language paths cost, both of them are the same for everyone.
