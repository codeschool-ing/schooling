---
format: 5
course: node
---

# node

**Node.js: JavaScript on the Server** · `co-1jq1sr3v` · 60 h declared · intermediate · 11 lessons · `backend` · paid

## Reach

In **1 track** — `backend`(5, choice *JavaScript / Node.js*).

**Reached through a choice**, never in sequence — so **no course after that fork may assume it was taken**.

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `javascript` and `git` — the language without the DOM, and version control.

**Leaves ready:** **nothing.** No course requires it.

## Shape

| | |
|---|---|
| declared hours | 60 h |
| lessons | 11 |
| **hours per lesson** | **5.45** |
| section budget | ~129, about 11.7 a lesson |
| exercises | ~610, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **Node** |
| browser · database | no · no |
| exercises **blocked** | **~400 (65%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~40 — the event loop on the server, streams and buffers, the cluster module, middleware |

## Ageing

**Moderate.** Express is stable and everything around it moves.

## Flags

**1 ·** **Eleven lessons for sixty hours — 11.7 sections each, among the widest in the catalogue.** Lesson 8 is the filesystem, streams *and* buffers. These are small courses wearing lesson titles.

**2 ·** **Its first lesson is the fork's note, written into the material** — *"Why master one language before jumping to another"* — and its last is *"A look at other server languages"*. Same shape the four frontend courses carry. **It is the cheapest of the four backend paths at 140 hours and the only one that explains the choice to the student who made it.**

**3 ·** **The shortest path of the fork, and nothing downstream knows which path was taken.** `apis`, `architecture`, `scale` and the rest of `backend` are written for a student holding one of four languages. Those courses name none, which is the right call and is the register's point about the fork.
