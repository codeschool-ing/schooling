---
format: 5
course: angular
---

# angular

**Front-end with Angular** · `co-a1f84tyn` · 90 h declared · intermediate · 25 lessons · `frontend` · paid

## Reach

In **1 track** — `frontend`(6, choice *Angular*).

**Reached through a choice**, never in sequence — so **no course after that fork may assume it was taken**.

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `javascript` and `git`.

**Leaves ready:** **nothing.** No course requires it.

## Shape

| | |
|---|---|
| declared hours | 90 h |
| lessons | 25 |
| **hours per lesson** | **3.60** |
| section budget | ~193, about 7.7 a lesson |
| exercises | ~910, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **Node and a browser** |
| browser · database | **yes** · no |
| exercises **blocked** | **~600 (70%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~65 — the injector hierarchy, change detection propagating, a reactive form tree, the router with guards and resolvers |

## Ageing

**Moderate.** Signals and zoneless change detection (lessons 6 and 7) are mid-migration, and the course says so.

## Flags

**1 ·** **Twenty-five lessons, the most of the four frameworks**, at 7.7 sections each — and it is the framework with the most surface, so the count is earned rather than inherited.

**2 ·** **Its last lesson looks at the others.** That is the same shape `react-ts`, `vue`, `angular` and `svelte` all carry, and it is the catalogue answering the choice problem inside the material instead of asking for a mechanism — see the register.

**3 ·** **Lesson 3 is the honest one** — *"Standalone components, and the NgModule code you will still meet"*. A framework mid-migration has to teach both the present and what the reader will open at work, and this is the only course in the four that carries a legacy lesson deliberately.
