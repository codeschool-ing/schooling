---
format: 5
course: observability
---

# observability

**Observability and Operations** · `co-07jej04e` · 80 h declared · advanced · 19 lessons · `infra` · paid

## Reach

In **4 tracks** — `data-platform`(4), `devops`(14), `networks-infra`(12), `software-architecture`(8).

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `cloud` — managed services and where a workload runs.

**Leaves ready:** nothing.

## Shape

| | |
|---|---|
| declared hours | 80 h |
| lessons | 19 |
| **hours per lesson** | **4.21** |
| section budget | ~171, about 9.0 a lesson |
| exercises | ~800, at the catalogue's density |


## Execution

| | |
|---|---|
| runtime | **a service to instrument and somewhere for the signals to land** |
| browser · database | dashboards are a browser interface, so a driven one, yes · no |
| exercises **blocked** | **0 as written** |
| exercises that would **improve** | about half. Reading a trace or a dashboard is reading data and needs no environment; instrumenting a service needs one |
| diagrams to draw | ~30 — signal paths, sampling, the trace/metric/log triangle |

## Ageing

**Moderate to severe.** The concepts are stable and every dashboard in the field is a product screen that moves.

## Flags

**1 ·** **Its summary contains its own design instruction**: *nothing is visible that was not made visible*. The course starts from instrumenting rather than from reading a dashboard, which is the harder half to teach and the one that needs an environment. Worth honouring rather than quietly inverting into a tour of tools.
