---
format: 5
course: testing-cicd
---

# testing-cicd

**Automated Testing and CI/CD** · `co-bxyd53wn` · 60 h declared · intermediate · 11 lessons · `backend` · paid

## Reach

In **6 tracks** — `backend`(10), `data-platform`(3), `devops`(12), `devsecops`(12), `mobile`(6), `qa`(12).

**Depends on it:** `secure-pipeline`

## Assumes, and leaves ready

**Assumes:** `git` — branches and pull requests, which every trigger in this course hangs off.

**Leaves ready:** the pipeline, for `secure-pipeline` — **the only edge from `backend` into `security`**, and it is the last link of the catalogue's deepest chain.

## Shape

| | |
|---|---|
| declared hours | 60 h |
| lessons | 11 |
| **hours per lesson** | **5.45** |
| section budget | ~129, about 11.7 a lesson |
| exercises | ~610, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **a CI runner** |
| browser · database | no · no |
| exercises **blocked** | **~400 (65%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~50 — the test pyramid, the doubles compared, a matrix run, blue-green and canary drawn as traffic |

## Ageing

**Moderate.** GitHub Actions and GitLab CI move; the strategies do not.

## Flags

**1 ·** **Six tracks — `backend`, `data-platform`, `devops`, `devsecops`, `mobile` and `qa` — which is the widest reach of any course in this batch after `python` and `javascript`.** It is the course that everybody who ships anything meets, and it has one dependent that sits at the end of the longest chain in the catalogue.

**2 ·** **Eleven lessons for sixty hours — 11.7 sections each.** Lesson 10 is blue-green, canary *and* feature flags. Wide, and this one has six tracks reading it.

**3 ·** **This repository is the worked example, again.** `.github/workflows/` runs matrix jobs, path-filtered triggers, secrets and a release that is a tag. **Fifth teach-from-itself case**, and the one with the widest reach.
