---
format: 5
course: go-back
---

# go-back

**Back-end with Go** · `co-kydp2jmw` · 70 h declared · intermediate · 17 lessons · `backend` · paid

## Reach

In **1 track** — `backend`(5, choice *Go*).

**Reached through a choice**, never in sequence — so **no course after that fork may assume it was taken**.

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `go-concurrency` — the standard library and `context`, which every handler in this course threads through.

**Leaves ready:** **nothing.** No course requires it.

## Shape

| | |
|---|---|
| declared hours | 70 h |
| lessons | 17 |
| **hours per lesson** | **4.12** |
| section budget | ~150, about 8.8 a lesson |
| exercises | ~700, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **the Go toolchain and a database** |
| browser · database | no · **yes** |
| exercises **blocked** | **~500 (70%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~45 — the middleware chain, a request through the layers, the sqlc pipeline, gRPC streaming |

## Ageing

**Low to moderate.** Lesson 1 names Go 1.22 routing, which dates it precisely; the four frameworks of lesson 4 move at their own pace.

## Flags

**1 ·** **Lesson 5 is the course's opinion and it is the reason to write it well** — *"When the standard library is enough and the framework gets in the way"*. That is the judgement a Go backend course exists to transmit, and it is one lesson against a lesson naming four frameworks.

**2 ·** **Seventeen lessons at 8.8 sections each**, the wide end of the language courses, against `go-production`'s 24 at 6.2. The same track position, the same hours, two different shapes — worth being deliberate about rather than letting the lesson counts decide.

**3 ·** **Third of four in the 290-hour Go path.** Nothing depends on it and it is reached by one track through one fork option.
