---
format: 5
course: python-back
---

# python-back

**Back-end with Python: Django and FastAPI** · `co-p6dkjkhn` · 60 h declared · intermediate · 17 lessons · `backend` · paid

## Reach

In **1 track** — `backend`(5, choice *Python*).

**Reached through a choice**, never in sequence — so **no course after that fork may assume it was taken**.

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `python` and `git`.

**Leaves ready:** **nothing.** No course requires it.

## Shape

| | |
|---|---|
| declared hours | 60 h |
| lessons | 17 |
| **hours per lesson** | **3.53** |
| section budget | ~129, about 7.6 a lesson |
| exercises | ~610, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **Python, a database and a queue** |
| browser · database | no · **yes** |
| exercises **blocked** | **~450 (70%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~45 — the Django request cycle, an ORM query becoming SQL, Pydantic validating, Celery with its broker |

## Ageing

**Moderate.** Django and FastAPI both move; lesson 1's comparison is the durable part.

## Flags

**1 ·** **It carries two frameworks and says so in lesson 1.** *"Choosing between Django and FastAPI: what each one assumes"* — then teaches both, five lessons each. That is honest and it doubles the writing; **the alternative is choosing one, and the sheet records that the choice was made the other way rather than skipped.**

**2 ·** **150 hours for the Python path, one of the two cheap options.** Behind the catalogue's biggest hub, which means a `backend` student choosing Python is also holding the prerequisite for nine other courses in other tracks. **No other fork option has that property**, and nothing in the track surfaces it.

**3 ·** **Seventeen lessons at 7.6 sections each** — the middle of the range, and the only backend language course whose shape is unremarkable.
