---
format: 5
course: svelte
---

# svelte

**Front-end with Svelte** · `co-64bbcncv` · 70 h declared · intermediate · 20 lessons · `frontend` · paid

## Reach

In **1 track** — `frontend`(6, choice *Svelte*).

**Reached through a choice**, never in sequence — so **no course after that fork may assume it was taken**.

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `javascript` and `git`.

**Leaves ready:** **nothing.** No course requires it.

## Shape

| | |
|---|---|
| declared hours | 70 h |
| lessons | 20 |
| **hours per lesson** | **3.50** |
| section budget | ~150, about 7.5 a lesson |
| exercises | ~700, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **Node and a browser** |
| browser · database | **yes** · no |
| exercises **blocked** | **~500 (70%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~55 — the compiler removing itself, runes against refs, form actions with JavaScript off, universal against server load |

## Ageing

**Moderate.** Lesson 4 is *"What Svelte 5 changed, and the Svelte 4 code still out there"* — a version boundary written into the syllabus, which dates the course precisely and honestly.

## Flags

**1 ·** **Seventy hours — the cheapest of the four options**, and a fifth less than React or Angular. Within a fork that is fine; it is worth noting only because `backend`'s equivalent spread is more than double.

**2 ·** **Its last lesson looks at the others.** That is the same shape `react-ts`, `vue`, `angular` and `svelte` all carry, and it is the catalogue answering the choice problem inside the material instead of asking for a mechanism — see the register.

**3 ·** **Lesson 14 is the idea no other framework course teaches** — form actions and progressive enhancement that works with JavaScript off. That is the course's distinctive claim and it is one lesson; the material should let it carry more weight than its position suggests.
