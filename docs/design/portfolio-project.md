---
format: 5
course: portfolio-project
---

# portfolio-project

**The Portfolio Project: From Brief to Deploy** · `co-3ax4ryfp` · 60 h declared · intermediate · 21 lessons · `career` · paid

## Reach

In **16 tracks** — `ai`(14), `backend`(14), `bi`(17), `cloud-engineering`(10), `data`(15), `data-science`(13), `dba`(14), `devops`(16), `devsecops`(18), `frontend`(11), `it-support`(8), `mobile`(9), `networks-infra`(14), `prompt`(5), `qa`(14), `security`(19).

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** **sixteen different things, and it may name none of them.** It is the penultimate course of all sixteen tracks that start a career, so the student before it has finished anything from `frontend` to `security` to `it-support`. The only honest assumption is that they can build something.

**Leaves ready:** the artefact `first-job` sends to employers. The two are one piece of work split in half, and nothing declares that relationship — no `requires`, no `links`.

## Shape

| | |
|---|---|
| declared hours | 60 h |
| lessons | 21 |
| **hours per lesson** | **2.86** |
| section budget | ~129, about 6.1 a lesson |
| exercises | ~610, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **the student's own, and that is the point** — lesson 15 deploys to a real free tier with a real domain |
| browser · database | no · no |
| exercises **blocked** | **the deliverable itself.** A portfolio project is a repository on somebody else's server, and no grader in this build or any planned one judges one |
| exercises that would **improve** | ~30 — the checklist items around it (a README, a licence, an empty state) are gradeable where the project is not |
| diagrams to draw | ~40 — a scope cut drawn as before and after, a one-person board, a commit history that reads |

## Ageing

**Moderate.** Free deployment tiers appear and vanish, GitHub's profile page changes shape. Lesson 15 and lesson 18 will need re-recording on somebody else's schedule.

## Flags

**1 ·** **The widest reach in the catalogue, wider than `git`.** Sixteen tracks, against `git`'s thirteen and `web-fundamentals`'s twelve — and unlike either, it is in every one of those tracks at the same place: second from the end. **Every student who finishes anything finishes through this course.** It has had none of the attention `web-fundamentals` has, and a weak course here is weak for the entire school at the moment the student is deciding whether the whole thing was worth it.

**2 ·** **It must be track-aware and the content model has no way to be.** Lesson 3 is literally "Choosing by track: what somebody hiring in your field actually opens" — the course knows it has sixteen audiences. Nothing in `content/` lets a course, a lesson or a section differ by the track that reached it. So either the material is generic, which is exactly what a portfolio course must not be, or the mechanism is built. **This is the second sheet in this batch to want it** — `design-patterns` wants it for four languages — which turns a nice-to-have into a decision with two courses behind it.

**3 ·** **Nothing graded, and the least graded course in the catalogue.** Of the eight graders, this course can use them only on its own periphery. The assessment that matters is a human reading a repository, and `C-27`'s honesty about what a course carries should extend to saying so on the page rather than shipping quizzes about portfolios.

**4 ·** **Sixty hours, paid, in sixteen tracks, and nothing requires it.** By the graph it is a leaf. By the funnel it is the last thing standing between a student and finishing. Those two readings disagree about how much it matters, and the funnel is the one that pays.
