---
format: 5
course: react-native
---

# react-native

**Mobile Apps with React Native** · `co-pnqyhg54` · 90 h declared · intermediate · 22 lessons · `mobile` · paid

## Reach

In **1 track** — `mobile`(4, choice *Multiplatform with React Native*).

**Reached through a choice**, never in sequence — so **no course after that fork may assume it was taken**.

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `javascript` — and not `react-ts`, which is a different track's fork option. A student arriving here may never have used React.

**Leaves ready:** **nothing.** No course requires it.

## Shape

| | |
|---|---|
| declared hours | 90 h |
| lessons | 22 |
| **hours per lesson** | **4.09** |
| section budget | ~193, about 8.8 a lesson |
| exercises | ~910, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **Node, Metro and a simulator or device** |
| browser · database | no · **yes, on the device** |
| exercises **blocked** | **~600 (75%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~60 — the bridge and the new architecture, Flexbox without CSS, list virtualisation, the animation thread |

## Ageing

**Severe.** Expo, Reanimated, TanStack Query and the new architecture all move quickly.

## Flags

**1 ·** **It requires `javascript` and teaches React from scratch**, because the `mobile` track never reaches `react-ts`. Lessons 1 to 9 rebuild what a `frontend` student already has. **That is correct for its own track and it is 90 hours that partly duplicate another 90-hour course** — the largest duplication the sweep has found between two courses that no student takes together.

**2 ·** **Lesson 20 is the fork's escape hatch** — *"EAS Build: credentials, and building without a Mac in the room"* — which is the only place in the catalogue where a course names the way round a platform wall. It matters for `ios-apps` too, and neither course cross-references it.

**3 ·** **Lesson 22 looks at Flutter and Kotlin Multiplatform**, which is the same closing move the four frontend framework courses make. **The pattern reaches five of the catalogue's six fork-option families**; the exceptions are the backend languages and the cloud providers.
