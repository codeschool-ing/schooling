---
format: 5
course: scale
---

# scale

**Scale and Observability** · `co-00kjjjk0` · 60 h declared · advanced · 12 lessons · `backend` · paid

## Reach

In **1 track** — `backend`(13).

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `architecture` — the patterns, before the limits that force them.

**Leaves ready:** **nothing.** It is the last subject course of `backend`.

## Shape

| | |
|---|---|
| declared hours | 60 h |
| lessons | 12 |
| **hours per lesson** | **5.00** |
| section budget | ~129, about 10.8 a lesson |
| exercises | ~610, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **a database that can be replicated, and a load generator** |
| browser · database | no · **yes, and lesson 4 wants five kinds** |
| exercises **blocked** | **~400 (70%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~55 — vertical against horizontal, sharding drawn, backoff with and without jitter, a trace across services |

## Ageing

**Low.** Lesson 5 names five databases and lesson 8 names OpenTelemetry; the reasoning is durable.

## Flags

**1 ·** **Twelve lessons for sixty hours — 10.8 sections each**, and lessons 4 and 5 are `nosql-operations` compressed into two. That course is 60 hours in `dba`; `backend` reaches neither it nor `db-performance`, so this is the only exposure. **Sixth compressed overview and correctly placed.**

**2 ·** **It is the second half of `architecture` by subject and a separate course by structure.** CAP appears in both. The prerequisite makes the ordering explicit, which is what stops the repetition being an accident — the same relationship `attacks-threats` and `secure-code` have.

**3 ·** **Last subject course of the track, and it is where the four language paths are furthest behind.** A student here has spent between 140 and 290 hours on a language and the same number of hours on everything since. The material may assume all of the latter and none of the former.
