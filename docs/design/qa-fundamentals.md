---
format: 5
course: qa-fundamentals
---

# qa-fundamentals

**Software Quality Fundamentals** · `co-ed3whn7n` · 60 h declared · beginner · 22 lessons · `quality` · paid

## Reach

In **1 track** — `qa`(2).

**Depends on it:** `manual-testing`

## Assumes, and leaves ready

**Assumes:** **nothing.** No `requires`, beginner, position 2 of `qa` behind `web-fundamentals`.

**Leaves ready:** the vocabulary the other four stand on. `manual-testing` requires it and the rest follow from that.

## Shape

| | |
|---|---|
| declared hours | 60 h |
| lessons | 22 |
| **hours per lesson** | **2.73** |
| section budget | ~129, about 5.9 a lesson |
| exercises | ~610, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **none** |
| browser · database | no · no |
| exercises **blocked** | **0** |
| exercises that would **improve** | ~25 |
| diagrams to draw | ~55 — the cost-of-defect curve, the V model, black against white box, the five whys, an Ishikawa diagram |

## Ageing

**None.** The models are decades old and the ones that matter — TDD, BDD, the agile cycle — are settled.

## Flags

**1 ·** **Sixty hours needing nothing at all**, and the fourth such course the sweep has found after `statistics`, `data-fundamentals` and `security-fundamentals`. It is the entry to a chain of five and everything after it is blocked, so **writing it first costs nothing and unlocks nothing** — which is worth being clear-eyed about rather than counting it as progress.

**2 ·** **Lesson 5 is the course arguing for its own profession** — *"The QA role on the team: not the guardian of 'you cannot ship'"* — and lesson 22 closes with *"metrics that turn into theatre"*. Those two frame everything between them, and they are judgement, not technique.

**3 ·** **The whole category is one linear chain in one track, and this is its head.** Nothing outside `qa` reaches any of the five. See the category note in the register: `quality` is the only self-contained category in the catalogue.
