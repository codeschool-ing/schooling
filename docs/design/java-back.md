---
format: 5
course: java-back
---

# java-back

**Back-end with Java and Spring** · `co-raa93xmv` · 70 h declared · intermediate · 23 lessons · `backend` · paid

## Reach

In **1 track** — `backend`(5, choice *Java*).

**Reached through a choice**, never in sequence — so **no course after that fork may assume it was taken**.

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `java-functional` — streams, concurrency and the standard library.

**Leaves ready:** **nothing.** No course requires it.

## Shape

| | |
|---|---|
| declared hours | 70 h |
| lessons | 23 |
| **hours per lesson** | **3.04** |
| section budget | ~150, about 6.5 a lesson |
| exercises | ~700, at the catalogue's density |

## Execution

| | |
|---|---|
| runtime | **a JDK, a build tool, a database and containers** (Testcontainers, lesson 19) |
| browser · database | no · **yes** |
| exercises **blocked** | **~500 (70%)** |
| exercises that would **improve** | the remainder |
| diagrams to draw | ~50 — the Spring context, dependency injection resolved, JPA's N+1, the build lifecycle |

## Ageing

**Moderate.** Spring Boot moves on a predictable cycle; the eight alternatives named across lessons 8, 12 and 16 move less predictably.

## Flags

**1 ·** **Twenty-three lessons and roughly nine of them name products** — Maven, Gradle, Bazel, Spring Boot, Quarkus, Javalin, Play, Micronaut, Hibernate, EBean, Flyway, Liquibase, SLF4J, Logback, Log4j2, TinyLog, JUnit, TestNG, Mockito, Testcontainers, REST Assured, JMeter, Cucumber. **The densest product list in the batch**, and it takes the `ai-models` treatment: text-first, short, cheap to re-render.

**2 ·** **It needs containers to teach its own testing lesson.** Testcontainers is lesson 19, which means this course's environment is a JDK *and* a container daemon — the only language course in the batch that needs both.

**3 ·** **The end of the 220-hour Java path.** Nothing depends on it, one track, one fork option.
