---
format: 5
course: networks
---

# networks

**Networks and Protocols** · `co-jgrx4b7q` · 60 h declared · intermediate · 10 lessons · `infra` · paid

## Reach

In **9 tracks** — `cloud-engineering`(3), `data`(3), `dba`(4), `devops`(5), `devsecops`(5), `it-support`(5), `networks-infra`(3), `security`(4), `software-architecture`(5).

**Depends on it:** `cloud`, `networks-addressing`, `networks-security`

## Assumes, and leaves ready

**Assumes:** `linux-terminal` — the shell and its diagnostic tools.

**Leaves ready:** packets, addressing, routing and diagnosis, for `cloud`, `networks-addressing` and `networks-security`. Through `cloud` it reaches nine more courses.

## Shape

| | |
|---|---|
| declared hours | 60 h |
| lessons | 10 |
| **hours per lesson** | **6.00** |
| section budget | ~129, about 12.9 a lesson |
| exercises | ~610, at the catalogue's density |


## Execution

| | |
|---|---|
| runtime | **shell**, for the diagnostic tools |
| browser · database | no · no |
| exercises **blocked** | **0** |
| exercises that would **improve** | ~a third — `ping`, `traceroute`, `dig`, `ss` and packet capture as `expected-output` |
| diagrams to draw | ~40 — topology, the layer stack, routing tables, capture reading |

## Ageing

**Very low.** Protocols outlive interfaces.

## Flags

**1 ·** **Ten lessons for sixty hours — 12.9 sections each, tied with `cloud` for the widest.** Same instruction: these are small courses wearing the word "lesson".

**2 ·** **Nine tracks and three dependents, and one of those dependents is `cloud`.** It sits behind more of the category than its own reach suggests.
