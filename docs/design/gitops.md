---
format: 5
course: gitops
---

# gitops

**GitOps, Artefacts and Secrets** · `co-e9t6kr9b` · 60 h declared · advanced · 12 lessons · `infra` · paid

## Reach

In **1 track** — `devops`(13).

**Depends on it:** **nothing**

## Assumes, and leaves ready

**Assumes:** `iac` — infrastructure described in version-controlled files.

**Leaves ready:** nothing.

## Shape

| | |
|---|---|
| declared hours | 60 h |
| lessons | 12 |
| **hours per lesson** | **5.00** |
| section budget | ~129, about 10.8 a lesson |
| exercises | ~610, at the catalogue's density |


## Execution

| | |
|---|---|
| runtime | **shell + git + a cluster** |
| browser · database | no · no |
| exercises **blocked** | **0 as written** |
| exercises that would **improve** | most of it, and it inherits `kubernetes`'s problem: the subject is a reconciliation loop against a live cluster |
| diagrams to draw | ~25 — the reconciliation loop, promotion between environments, rollback paths |

## Ageing

**Moderate.** The pattern is stable; the tools that implement it are young and their interfaces move.

## Flags

**1 ·** **Twelve lessons for sixty hours — 10.8 sections each.** Wide lessons, like `cloud` and `networks`, and the same instruction follows: each needs an internal arc.

**2 ·** **One track, one position, no dependents.** It is the end of `devops` and nothing is waiting on it, which makes it the cheapest course in the category to defer.
