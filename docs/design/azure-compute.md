---
format: 5
course: azure-compute
---

# azure-compute

**Azure: Compute and Applications** · `co-yhedd1fw` · 60 h declared · intermediate · 22 lessons · `infra` · paid

## Reach

In **1 track** — `cloud-engineering`(9, escolha *Azure*).

**Reached through a choice**, never in sequence — so **no course after that fork may assume it was taken**.

**Depends on it:** `azure-operations`

## Assumes, and leaves ready

**Assumes:** `azure-foundations` and `docker` — accounts, identity, networking and what a container is.

**Leaves ready:** the compute surface of Azure, for `azure-operations`.

## Shape

| | |
|---|---|
| declared hours | 60 h |
| lessons | 22 |
| **hours per lesson** | **2.73** |
| section budget | ~129, about 5.9 a lesson |
| exercises | ~610, at the catalogue's density |


## Execution

| | |
|---|---|
| runtime | **a real Azure account** — not a runtime |
| browser · database | the console is a browser interface, so a recorded or driven one, yes · no |
| exercises **blocked** | **0 as written** — but see the flag: an exercise that would ask a student to create a resource has nowhere to run |
| exercises that would **improve** | most of the practice. `expected-output` against the Azure CLI is the natural shape and needs credentials and a billable account, which is a different problem from a sandbox |
| diagrams to draw | ~40 — account and network topology, service boundaries, permission chains |

## Ageing

**Severe.** The screen is Azure's console, and a console changes on its owner's schedule — the one kind of material that goes wrong while its script stays right. Whatever can be taught from an annotated still rather than a recording should be, and what cannot is declared per lesson.

## Flags

**1 ·** **Reached from one track only**, and only as the middle of the provider chain in `cloud-engineering`. Nobody meets this course without having done its foundations first, which is the one place in the family where a sheet may assume its predecessor completely.

**2 ·** **One design, three instances** — and here the lesson counts differ (24, 22, 23), so the parallel is in the shape rather than in the number.
