# The provider binaries and the lock's local cache.
.terraform/

# NO STATE IN THE REPOSITORY. It lives in the bucket, and a local copy here
# would be a second answer to "what exists" — plus every value the state file
# happens to carry, in a place nobody thinks of as a secret store.
*.tfstate
*.tfstate.*
terraform.tfvars
