{
  "name": "schooling-browser-suites",
  "version": "0.0.0",
  "private": true,
  "description": "The checks that need a real browser. The product is Go; this file exists only so the graph test has a browser to open, and it is deliberately the whole of the JavaScript toolchain — the interface itself has no build step and no dependencies.",
  "type": "module",
  "scripts": {
    "graph-test": "node tools/graph-test/graph-test.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.0"
  }
}
