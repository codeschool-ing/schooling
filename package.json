{
  "name": "schooling-browser-suites",
  "version": "0.0.0",
  "private": true,
  "description": "The checks that need a real browser: the graph is measured in one and every screen is put through axe. The product is Go, the interface has no build step and no dependencies of its own, and this file is deliberately the whole of the JavaScript toolchain.",
  "type": "module",
  "scripts": {
    "graph-test": "node tools/graph-test/graph-test.mjs",
    "a11y-test": "node tools/a11y-test/a11y-test.mjs",
    "mfa-test": "node tools/mfa-test/mfa-test.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "playwright": "1.56.0"
  }
}
