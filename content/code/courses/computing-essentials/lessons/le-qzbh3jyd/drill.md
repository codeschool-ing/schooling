---
title: Practice
version: 1
---

One difference produces the rest: the document is on a server and the browser is a window onto
it. No save, a history instead of an undo stack, an address instead of a path, and a copy you do
not hold.

Half of these questions are about sharing, because that is the half where a mistake reaches
somebody else. When one describes a setting, ask who it lets in rather than what it is called.
