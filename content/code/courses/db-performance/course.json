{
  "id": "db-performance",
  "name": "Database Performance and Scale",
  "category": "data",
  "level": "advanced",
  "hours": 70,
  "summary": "Making the database answer fast when the load arrives — and knowing which of the three suspects is actually slow.",
  "requires": [
    "db-administration"
  ],
  "prerequisites": "Requires administration: you tune a server you already know how to configure.",
  "lessons": [],
  "syllabus": [
    "Measuring before touching: which query, how often, for how long",
    "Execution plans, statistics and the decisions the planner makes",
    "Indexes beyond the B-tree, and the ones that cost more than they save",
    "Locks, concurrency and the isolation level under real load",
    "Partitioning, sharding and read replicas",
    "Capacity, benchmarking and a load test that means something"
  ],
  "topics": [
    "The three suspects: the query, the schema and the machine",
    "Query statistics: finding what costs the most in total, not per run",
    "EXPLAIN and EXPLAIN ANALYZE: reading the plan instead of guessing",
    "Sequential scan, index scan and bitmap scan: when each is right",
    "Nested loop, hash join and merge join, and why the planner picks one",
    "Estimated against actual rows: the sign that the statistics are wrong",
    "Histograms, distinct counts and extended statistics",
    "B-tree, hash, GIN, GiST and BRIN: the index for the question",
    "Partial indexes and indexes on an expression",
    "Covering indexes and the index-only scan",
    "Unused, duplicated and overlapping indexes: the tax they charge on writes",
    "Locks: row, table and advisory, and what blocks what",
    "Finding who is blocking whom, while it is happening",
    "Isolation levels under real concurrency, and the serialisation failure",
    "Long transactions and the cost they impose on everything else",
    "Connection saturation, and why pooling beats raising the limit",
    "Declarative partitioning: by range, by list and by hash",
    "Partition pruning, and the query that ignores the partitioning",
    "Sharding: when it becomes unavoidable, and what it costs forever",
    "Read replicas, replication lag and reading your own write",
    "A cache in front of the database, and the invalidation it inherits",
    "Benchmarking with a workload that reproduces the real one",
    "Capacity: growth, headroom and an alert that arrives before the wall",
    "The optimisation that was not needed: measuring the gain afterwards"
  ]
}
